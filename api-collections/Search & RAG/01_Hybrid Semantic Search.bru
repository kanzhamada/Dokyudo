meta {
  name: Hybrid Semantic Search
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/search
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

body:json {
  {
    "query": "lokal",
    "limit": 2
  }
}

docs {
  Performs tenant-safe hybrid search using Upstash Vector + Postgres FTS, merged via RRF (Reciprocal Rank Fusion).
}
