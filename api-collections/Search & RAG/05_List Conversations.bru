meta {
  name: List Conversations
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/rag/conversations?limit=20&cursor={{nextCursor}}
  body: none
  auth: bearer
}

params:query {
  limit: 20
  cursor: {{nextCursor}}
}

auth:bearer {
  token: {{accessToken}}
}
