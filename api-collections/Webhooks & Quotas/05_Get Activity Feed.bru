meta {
  name: Get Activity Feed
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/activities
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

docs {
  Retrieves the tenant's latest activity feed (ingestions, searches, chat turns, webhook deliveries) using cursor-based pagination.
}
