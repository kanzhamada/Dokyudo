meta {
  name: Logout User
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/auth/logout
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

docs {
  Invalidates the user's session globally using the Supabase Admin API.
}
