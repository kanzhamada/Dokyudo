info:
  name: List Webhook Logs
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/api/admin/webhook-logs"
  headers:
    - name: Authorization
      value: "Bearer {{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Admin operations dashboard endpoint to query all tenant webhook delivery histories.
