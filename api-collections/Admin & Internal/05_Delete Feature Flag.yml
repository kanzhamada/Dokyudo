info:
  name: Delete Feature Flag
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/api/admin/feature-flags/{{flagId}}"
  headers:
    - name: Authorization
      value: "Bearer {{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Admin route to delete a feature flag.
