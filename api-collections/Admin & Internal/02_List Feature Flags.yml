info:
  name: List Feature Flags
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/admin/feature-flags"
  headers:
    - name: Authorization
      value: "Bearer {{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Admin route to retrieve all registered system feature flags.
