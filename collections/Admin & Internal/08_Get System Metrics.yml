info:
  name: Get System Metrics
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/api/admin/metrics"
  headers:
    - name: Authorization
      value: "Bearer {{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Exposes system counters, database connection states, and AI model timings for the admin panel dashboard.
