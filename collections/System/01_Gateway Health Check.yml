info:
  name: Gateway Health Check
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/health"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Basic endpoint to check Gateway API service health status, check DB pools and Redis readiness.
